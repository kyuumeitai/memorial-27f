import styled from 'styled-components'
import theme from '@/styles/theme'

const StEmbedFacebook = styled.div`
  max-width: ${theme.normalWidth};
  margin: ${theme.spacing} auto;
`

export { StEmbedFacebook }