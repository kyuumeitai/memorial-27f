import styled from 'styled-components'
import theme from '@/styles/theme'

const SharerWrapper = styled.div`
  margin-block-start: ${theme.spacing};
  margin-block-end: ${theme.spacing};
`

export { SharerWrapper }